---
description: Decompose a goal into a Conductor dependency tree — richly-briefed tasks gated by blockers.
---

Use the conductor-worker skill (planning section). Decompose the goal below into a task network on the
Conductor board: create tasks with complete self-contained briefs (description, context items,
required capabilities, priority), wire `add_dependency` edges so blockers gate execution order, and
set subtasks where work decomposes further. Goal:
$ARGUMENTS
